import { Box, Typography } from "@mui/material"

export const NavBar = () =>{
    return(
        <Box>
            <Typography>
                NavBar
            </Typography>
        </Box>
    )
}